# Scripts

- `train.py`: baseline transformer regressor training (placeholder)
- `eval.py`: baseline evaluation (MSE)
- `portfolio.py`: portfolio curve generator (simple equal-weight long-short)

For the hierarchical model, integrate `HierarchicalBertRegressor` into `train.py`.
